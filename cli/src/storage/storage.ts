import { capsuled } from "./capsuled";
import { capsules } from "./capsules";
import { keys } from "./keys";
import { log } from "./log";

export const storage = {
    capsules: capsules,
    capsuled: capsuled,
    keys: keys,
    log: log,
}